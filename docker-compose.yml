services:
  spectra:
    build: .
    container_name: spectra
    ports:
      - "8080:8080"
    environment:
      AI_PROVIDER: ${AI_PROVIDER:-local}
    volumes:
      - ./data:/app/data
      - ./inbox:/app/inbox
      - ./processed:/app/processed
    restart: unless-stopped
